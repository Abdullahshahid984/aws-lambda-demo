pipeline {
  agent  any
  tools {
    sonarQubeScanner 'SonarScanner'
  }
  stages {
    stage('SonarQube Scan') {
      steps {
        withSonarQubeEnv('sq1') {
          sh 'sonar-scanner -Dsonar.projectKey=lambda-function -Dsonar.sources=. -Dsonar.login=squ_ea14e84623a1cb09584c9445c1e4be71141820b3'
        }
      }
    }
  }
